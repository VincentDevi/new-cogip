import React from "react";

export const InvoiceCount = () =>{
    // still need to call another component who holds the data of
    // how many invoices we have, need to play with state so 
    // whith redux and need to make a fetch to my api.
    return (
        <div>
            <h2>Invoices</h2>
            
        </div>
    );
}